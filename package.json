{
  "name": "fit-o-charity-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd backend && npm install && npx tsc && cd ../frontend && npm run build"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "24.x"
  }
}
